联合查询：将多次查询（多条select语句），在记录上进行拼接（字段不会增加）

语法：
多条select语句构成：每一条select语句获取的字段数必须严格一致
但是字段类型无关
select语句1
union[选项]
select 语句

union选项:与select选项一样有两个
all 保留所有（不管重复）

distinct：去重（整个重复）：默认


联合查询的意义分为两种：
1、查询同一张表，但是需求不同：查询学生信息，男生身高升序，
   女生身高降序
2、多表查询：多张表的结构是完全一样，保存的数据（结构）也是一样的

order by使用：

a:在联合查询中，order by不能直接使用，需要对查询语句用括号才行
b:若要order by生效：必须搭配limit：limit使用限定的最大数即可






内连接 外连接 左连接 右连接 笛卡尔积




子查询

定义：sub query 查询是在某个结果之上进行的
   一条select语句包含了另一条select语句

分类：按位置分类 按结果分类

按位置分类：

from子查询

where子查询

exists子查询


按结果分类：根据子查询得到的数据来进行分类

标量子查询：子查询得到的结果是一行一列 

列子查询：子查询得到的结果是一列多行
需要使用in作为条件匹配，其实在mysql中还有几个类似的条件  all some any
all    all====in 其中一个即可   
some   any=====some ：any和some是一样的
any    =any ====为全部 
  


行子查询：子查询得到的结果是多列一行

行子查询返回的结果可以是一行多列（多行多列）


这三个子查询出现位置是在where之后
----------------------------
表子查询：子查询得到的结果是多行多列

表子查询返回的结果是当做二维表来使用

exists：是否存在的意思
exist子查询就是用来判断某些条件是否满足（跨表）

exists是接在where之后；exits返回的结果只有0和1
---------------------
视图 view

视图：view 是一种有结构（有行有列）但是结构中不真实存放实际数据
虚拟表的结构来源不是自己定义，而是从对应的基表中产生（视图的数据来源）
---------------
创建视图

语法
create view  视图名字 as select;
备注：select语句可以普通查询；可以是连接查询；也可是联合查询；
还可以是子查询

创建单表视图：基表只有一个
创建多表视图：基表来源至少两个
----------------
查看视图：查看视图的结构
视图是一张虚拟表：表，表的所有的查看方式都是适用于视图
 show tables 【like】/desc 视图名
show create table 视图名

视图比表还是有一个关键字的区别：view 查看表（视图）的创建语句的时候可以使用view关键字

-----------------

使用视图

使用视图主要为了查询：将视图当做表一样查询即可

视图执行：其实本质就是执行封装的select语句
----------------
修改视图
视图本身是不可修改，但是视图的来源是可以修改的。

修改视图：修改视图本身的来源语句（select语句）
alter view 视图名字 as 新的select语句

--------------------

删除视图
drop veiw 视图名字；

--------------
视图意义

1、视图可以节省sql语句：将一条复杂的查询语句使用视图进行保存，
以后可以直接对视图进行操作

2、数据安全，视图操作主要是针对查询的，如果对视图结构进行处理（
删除），不会影响基表数据（相对安全）

3、视图往往是在大项目中使用，而且是多系统使用，可以对外提供
有用的数据，但是隐藏关键（无用）的数据，数据安全

4、视图可以对外提供友好型：不同的视图可以提供不同的数据，
对外好像是专门的设计

5、视图可以更好的进行权限的控制（grant  revoke）

------------------
视图的数据操作

视图是可以进行数据写操作，但是有很多限制，将数据直接在视图
上进行操作

新增数据：数据新增直接对视图来进行 
1、多表视图是不能进行新增数据
2、单表可以插入数据

---------------
删除数据

1、多表视图不能删除数据
1、单表视图是可以删除数据
----------------
更新数据
理论上不能单表视
------
视图算法


-------------
事务

需求：有一张银行账户，有A用户给B用
户转账，A账户先减少，B账户增加
但是A操作完之后就断电了

解决方案：A减少钱，但是不要立刻修改数据表，B收到钱之后，
同时修改数据表
---------
事务安全
事务：transaction 一系列要发生的连续的操作
事务安全：一种保护连续操作同时满足（实现）的一种机制

------
事务操作
事务操作分为两种：自动事务（默认），手动事务

手动事务的操作流程：
1、开启事务：告诉系统有以下的操作（写）不要直接写入数据表
先存放到事务日志 start transaction

2、操作事务：a 李四账
户减少1000
             b 张三账户增加1000

3、关闭事务：选择性的将日志文件中操作的结果保存到数据表（同步）
或者说直接清空事务日志（原来操作全部清空）
  a：提交事务：同步数据表（操作成功）：commit
  b：回滚事务：直接清空日志表（操作失败）：rollback

------
事务原理

事务操作原理：事务开启之后，所有的操作都会临时保存到事务日志，
事务日志只有在得到commit命令才会同步到数据表，其他任何清空都会清空（rollback、断电、断电连接）

-----
回滚点：

定义：在某个成功的操作完成之后，后续的操作有可能成功、也有可能失败，但是不管已经成功还是失败，前面的操作都已经成功；可以在当前
成功的位置，设置一个点：可以供后续失败操作返回到该位置，
而不是返回所有的操作，这个点就称之为回滚点

设置回滚点：savepoint spt；
回到回滚点：rollback to 回滚点的名字

----------------------
自动事务处理
mysql中：默认的都是自动事务处理，用户操作完会立即同步到数据表总

自动事务：系统通过autocommit变量控制
 show variables like 'autocommit';

关闭事务自动提交：set autocommit=off /0

----------------------------
事务特征
事务具有四大特征：ACID
A：atomic原子性，事务的整个操作是一个整体，不可分割，要么全部成功，要么全部失败。

C：consistence 一致性，数据操作前后，数据表中的数据没有变化

I：isolation:事务操作是相互隔离不受影响

D: durability  持久性，数据一旦提交，不可改变，永久的改变数据表结构

--------------------
锁机制：innodb默认的是行锁，但是如果在事务操作过程中，没有使用索引，那么系统就会自动权标检索数据，自动升级为表

行锁：只有当前行被锁住，别的用户不能操作

表锁：整张表被锁住，别的用户不能操作
----------------
变量

变量分为两种：系统变量和自定义变量

系统变量
定义：系统定义好的变量，大部分的时候用户根本不需要使用系统变量
系统变量时用来控制服务器的表现 如：autocommit auot_increment_increment
---------------------------
查看系统变量
show variables; 查看系统变量

查看具体变量值：任何一个有数据返回的内容都是有select查看

select @@变量名

------------
修改系统变量

修改系统变量分为两种形式：会话级别和全局级别

会话级别：临时修改，当前客户端当次有效
set 变量名=值；set @@变量名=值；

全局级别：一次修改，永久生效（对所有客户端都生效）
set global 变量名=值

如果对方（其他）客户端当前已经连接上服务器，那么当次修改无效
要退出重新登录才能生效。

--------------
自定义变量

定义：系统为了区别系统变量，规定用户自定义变量必须使用
一个@符号

set @变量名=值

自定义变量也是类似系统变量查看
select @变量名

在mysl中 = 会默认的当做比较符号处理（很多地方）
mysql中为了区分比较和赋值的概念：重新定义了一个新的赋值符号 ：
 ‘：=’

mysql中允许从数据表中获取数据，然后赋值给变量：两种方式

一、边赋值，边查看结果
select @变量名:=字段名 from 数据源：
从字段中取值赋值给变量名，如果使用=会变成比较

二、只有赋值不看结果：要求严格
    数据记录最多只允许获取一条：mysql不支持数组

select 字段列表 from 表名 into 变量列表

所有自定义的变量都是会话级别：当前客户端当次连接有效

所有自定义变量是不区分数据库（用户级别）









